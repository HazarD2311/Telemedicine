<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.surfstudio.telemedicine">

    <application
        android:name="ru.surfstudio.telemedicine.app.App"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="ru.surfstudio.telemedicine.ui.screen.main.MainActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ru.surfstudio.telemedicine.ui.screen.splash.SplashActivityView" />

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
